# Vim Syntax highlighting for NStack .nml files

Extract the `syntax` and `ftdetect` to somewhere on your vim path, e.g. `~/.vim`.

Syntax highlighting will be automatically applied to files of extension `.nml`
